{
  "name": "vite-sealnext",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "sass scss/:css/ && vite build && cp -R js dist",
    "preview": "vite preview",
    "sass": "sass scss/:css/ --watch"
  },
  "devDependencies": {
    "sass": "^1.69.5",
    "vite": "^4.4.5"
  }
}
